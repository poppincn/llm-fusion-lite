# Era Fusion — provider credentials & settings.
#
# Copy this to ONE of:
#   • ./.env                      (repo-local, git-ignored)
#   • ~/.era-fusion/.env          (machine-wide, used by `fuse` from any directory)
# …or just `export` these in your shell profile. Real environment variables
# always win over .env files; .env never overrides an already-set variable.
#
# Set at least ANTHROPIC_API_KEY. The other two unlock cross-provider fusion.

ANTHROPIC_API_KEY=        # https://console.anthropic.com/  → API Keys
OPENAI_API_KEY=          # https://platform.openai.com/api-keys
GOOGLE_API_KEY=          # https://aistudio.google.com/apikey   (GEMINI_API_KEY also accepted)

# Optional overrides
# ERA_FUSION_HOME=~/.era-fusion   # where config.json, fusion.db, and .env live
# ERA_FUSION_PORT=8787            # server port
